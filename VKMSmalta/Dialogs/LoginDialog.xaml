<Window x:Class="VKMSmalta.Dialogs.LoginDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VKMSmalta.Dialogs"
        xmlns:viewModel="clr-namespace:VKMSmalta.Dialogs.ViewModel"
        mc:Ignorable="d"
        Title="LoginDialog" Height="100" Width="300"
        WindowStartupLocation="CenterScreen"
        d:DataContext="{d:DesignInstance Type=viewModel:LoginDialogViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Логин: "/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Path=Login}"/>
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Пароль: "/>
        <PasswordBox Grid.Row="1" Grid.Column="1" x:Name="passwordBox"/>
        <Button Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Content="ОК" Command="{Binding Path=ClickCommand}"/>
    </Grid>
</Window>
